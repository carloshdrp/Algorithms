const a = [
    [1, 2, 3],
    [4, 5, 6],
    [7, 8, 9],
];

function toRigth(a) {
    start = 2;
    while (start >= 0) {
        // console.log(a[start][start]);
        start--;
    }
}

toRigth(a);

console.log(Math.abs(-2) + Math.abs(+4));
